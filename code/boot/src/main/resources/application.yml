spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/meteoservice
  kafka:
    bootstrap-servers: kafka:9092
server:
  port: 8080
kafka:
  topic: my-Topic
# Open Meteo API configuration
openmeteo:
  api:
    url: https://api.open-meteo.com/v1/forecast
springdoc:
  swagger-ui:
    path: /swagger-ui.html
logging:
  level:
    root: INFO